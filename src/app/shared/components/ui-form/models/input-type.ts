export enum UIInputType {
    TEXT = 'text',
    NUMBER = 'number',
    SELECT = 'select',
    CHECKBOX = 'checkbox',
    RADIO_BUTTON = 'radio-button',
    TEXTAREA = 'textarea',
    DATE = 'date',
    FILE = 'file'
}